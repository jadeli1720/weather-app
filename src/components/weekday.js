import React from 'react';

const Weekday = ({daily}) => {
    console.log("weekday",daily)
    return (
        <div>
            {/* This is from WeeklyList */}
            
        </div>
    )
};

export default Weekday;